[pytest]
asyncio_mode = auto
testpaths = tests
python_files = test_*.py
addopts = -v --strict-markers
markers =
    postgresql: requires an isolated PostgreSQL instance (pgserver); skipped only if pgserver is unavailable
    unit: unit tests
    integration: integration tests
